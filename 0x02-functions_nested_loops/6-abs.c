#include "holberton.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: character
 * Return: returns the absolute value
 */
int _abs(int n)
{

if (n == 0)
{
	return (n);
}
else if (n > 0)
{
	return (n);
}
else
{
	return (n * -1);
}
_putchar('\n');
}
